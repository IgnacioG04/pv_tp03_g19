import Nav from './Nav';

const Header = () => {
    return (
        <header className="main-header">
            <div className="logo">Gestión Educativa</div>
            <Nav />
        </header>
    );
};

export default Header;
